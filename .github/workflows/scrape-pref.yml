name: 3) 都道府県指定取得 (minkou + gaccom)

on:
  workflow_dispatch:
    inputs:
      pref_minkou:
        description: 'minkou用 都道府県（例: 東京都 / tokyo / 大阪府 / osaka）'
        required: true
      pref_gaccom:
        description: 'gaccom用 都道府県コード（例: 13）※ minkou と同じ都道府県を指定'
        required: true
      delay:
        description: 'アクセス間隔（秒）'
        required: false
        default: '2.0'
      workers:
        description: '並列ワーカー数'
        required: false
        default: '2'
      force:
        description: 'minkou: 取得済みの市区町村も再取得する（true / false）'
        required: false
        default: 'false'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  scrape-pref:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: uv sync

      - name: minkou - Scrape ${{ github.event.inputs.pref_minkou }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          FORCE_FLAG=""
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            FORCE_FLAG="--force"
          fi
          uv run python scrapers/minkou/run.py \
            --pref "${{ github.event.inputs.pref_minkou }}" \
            --delay "${{ github.event.inputs.delay || '2.0' }}" \
            --workers "${{ github.event.inputs.workers || '2' }}" \
            --auto \
            $FORCE_FLAG

      - name: gaccom - Scrape ${{ github.event.inputs.pref_gaccom }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          uv run python scrapers/gaccom/run.py \
            --pref "${{ github.event.inputs.pref_gaccom }}" \
            --delay "${{ github.event.inputs.delay || '2.0' }}" \
            --workers "${{ github.event.inputs.workers || '2' }}"
